apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: dhakacart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: dhakacart-frontend
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
